# Mental Maths v1.22.0

## Highlights
- Forced faster app updates by promoting waiting service workers immediately.
- Added automatic one-time reload on service worker controller change so users run the latest app build.
- Switched navigation requests in the service worker to network-first to reduce stale shell loads.

## Files touched
- `js/app-core.js`
- `sw.js`
- `js/config.js`
- `js/versions.js`
